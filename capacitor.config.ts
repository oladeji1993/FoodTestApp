import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'foodCategory',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
